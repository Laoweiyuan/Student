server:
  port: 8080

spring:
  datasource:
    dynamic:
      primary: User
      strict: false
      datasource:
        User:
          url: jdbc:mysql://localhost:3306/user?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
          username: root
          password: tangmylp520
          driver-class-name: com.mysql.cj.jdbc.Driver
        Student:
          url: jdbc:mysql://localhost:3306/mystudent?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
          username: root
          password: tangmylp520
          driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.jwt.entity


jwt:
  secret: ThisIsASecretKeyForJWT_MustBeLongEnough
  expiration: 6000000
